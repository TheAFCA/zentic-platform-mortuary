{
  "name": "@zentic/shared-types",
  "version": "0.1.0",
  "description": "Shared TypeScript types for ZENTIC.pro (backend + frontend)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
